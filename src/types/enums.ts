export enum GameSettings {
  TIME = 'time',
  SPEED = 'speed',
  QUANTITY = 'quantity',
}

export const SpeedLabels = {
  0: [700, 2000] as [number, number],
  1: [550, 1200] as [number, number],
  2: [400, 800] as [number, number],
};
